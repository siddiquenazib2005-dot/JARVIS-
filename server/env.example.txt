# AURIX backend configuration
# Copy this file to .env (or paste the values into your host's environment
# settings) and fill in whatever keys you actually have. At least one required.

# Optional app token: when set, the Android app must send the same value
AURIX_APP_TOKEN=

# LLM provider keys (any subset; routing adapts automatically)
GROQ_API_KEY=
CEREBRAS_API_KEY=
OPENROUTER_API_KEY=
MISTRAL_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional extra keys for quota pooling on the same provider
# GROQ_API_KEY_2=
# GEMINI_API_KEY_2=

# Runtime
PORT=8080
AURIX_DATA_DIR=./data
