name: Build and Test AURIX Debug APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  static-analysis:
    name: Repository static-analysis agents
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run the static-analysis agents
        # Pure-Python checks (no JDK needed), fast fail so broken refs and
        # manifest/symbol mismatches block the build job's 20+ minute run.
        run: |
          set -o pipefail
          python3 tools/agents/agent1_symbols.py
          python3 tools/agents/agent2_manifest.py
          python3 tools/agents/agent3_structure.py
          python3 tools/agents/agent4_wiring.py
          python3 tools/agents/agent5_unused.py
          python3 tools/agents/agent6_lambda_returns.py
          python3 tools/agents/agent7_members.py
          echo "All static-analysis agents passed."

  build:
    needs: static-analysis
    name: Unit tests and debug APK
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Run unit tests and build debug APK
        run: |
          set -o pipefail
          gradle :app:testDebugUnitTest :app:assembleDebug --no-daemon --stacktrace 2>&1 | tee build-output.log

      - name: Kotlin compiler errors only
        if: failure()
        run: |
          echo "=============== KOTLIN ERRORS ==============="
          grep -E "^e: |^e:" build-output.log | sed 's|file:///home/runner/work/[^/]*/[^/]*/||' || echo "(no e: lines - failure was not a Kotlin compile error)"
          echo "=============== TEST FAILURES ==============="
          grep -E "FAILED|There were failing tests|AssertionError" build-output.log | tail -60 || true
          echo "=============== WARNINGS (last 20) ==============="
          grep -E "^w: " build-output.log | sed 's|file:///home/runner/work/[^/]*/[^/]*/||' | tail -20 || true

      - name: Upload full build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: AURIX-build-log
          path: build-output.log
          if-no-files-found: ignore

      - name: Upload unit-test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: AURIX-unit-test-reports
          path: |
            app/build/reports/tests/
            app/build/test-results/
          if-no-files-found: ignore

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: AURIX-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
