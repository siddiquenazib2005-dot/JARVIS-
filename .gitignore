*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

keystore/

# JARVIS secrets (dev environment config — never commit)
secrets.properties
app/secrets.properties

# Build artifacts — source repo only
dist/
*.apk
/.kotlin
*.aab

# device-test.sh output (written to repo root, not /tmp)
device-install.log
device-artifacts/

# Whole app build dir (tracked build artifacts from an early commit)
app/build/
server/node_modules
server/.env
server/data
